[project]
name = "ibm-governed-tool-catalog"
version = "0.0.1"
description = "Scaffold: Governed Tool Catalog (independent IBM lens)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
